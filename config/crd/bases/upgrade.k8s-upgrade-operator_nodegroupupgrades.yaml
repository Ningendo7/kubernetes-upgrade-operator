---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: nodegroupupgrades.upgrade.k8s-upgrade-operator
spec:
  group: upgrade.k8s-upgrade-operator
  names:
    kind: NodeGroupUpgrade
    listKind: NodeGroupUpgradeList
    plural: nodegroupupgrades
    singular: nodegroupupgrade
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodeGroupUpgrade is the Schema for the nodegroupupgrades API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NodeGroupUpgrade
            properties:
              batchSize:
                format: int32
                type: integer
              drain:
                description: DrainPolicy controls how nodes in this group are cordoned/drained.
                properties:
                  deleteEmptyDirData:
                    default: true
                    type: boolean
                  force:
                    default: false
                    description: force, if true, force-evicts pods after timeoutSeconds
                      instead of pausing.
                    type: boolean
                  gracePeriodSeconds:
                    format: int32
                    type: integer
                  ignoreDaemonSets:
                    default: true
                    type: boolean
                  timeoutSeconds:
                    format: int32
                    type: integer
                type: object
              hold:
                description: |-
                  hold is set/cleared by the parent KubernetesUpgrade controller to
                  sequence control-plane-before-workers. Not normally hand-edited.
                type: boolean
              maxUnavailable:
                anyOf:
                - type: integer
                - type: string
                x-kubernetes-int-or-string: true
              nodeSelector:
                description: nodeSelector is how group membership is computed live,
                  each reconcile.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              nodes:
                description: nodes is a stable snapshot of member node names, used
                  for ordering/progress tracking.
                items:
                  type: string
                type: array
              paused:
                type: boolean
              provider:
                description: ProviderType identifies which provider adapter manages
                  this group's nodes.
                enum:
                - Kubeadm
                - AWSEKSManagedNodeGroup
                - AWSAutoScalingGroup
                - LinodeLKE
                - Generic
                type: string
              providerRef:
                description: |-
                  providerRef carries provider-specific identifiers. Exactly one variant
                  should be set, matching Provider.
                properties:
                  awsASG:
                    properties:
                      asgName:
                        type: string
                      region:
                        type: string
                    required:
                    - asgName
                    - region
                    type: object
                  awsEKS:
                    properties:
                      clusterName:
                        type: string
                      nodeGroupName:
                        type: string
                      region:
                        type: string
                    required:
                    - clusterName
                    - nodeGroupName
                    - region
                    type: object
                  linodeLKE:
                    properties:
                      clusterID:
                        type: string
                      poolID:
                        type: string
                    required:
                    - clusterID
                    - poolID
                    type: object
                type: object
                x-kubernetes-validations:
                - message: only one providerRef variant may be set
                  rule: (has(self.awsEKS)?1:0)+(has(self.awsASG)?1:0)+(has(self.linodeLKE)?1:0)<=1
              role:
                description: NodeGroupRole says whether this group is control-plane
                  or worker nodes.
                enum:
                - ControlPlane
                - Worker
                type: string
              strategy:
                description: NodeGroupStrategy is how nodes in this group get upgraded.
                enum:
                - InPlace
                - Replace
                type: string
              targetVersion:
                description: targetVersion is always a single-minor-version hop, set
                  by the parent KubernetesUpgrade.
                type: string
            required:
            - provider
            - role
            - strategy
            - targetVersion
            type: object
          status:
            description: status defines the observed state of NodeGroupUpgrade
            properties:
              conditions:
                description: conditions represent the current state of the NodeGroupUpgrade
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              message:
                type: string
              nodeProgress:
                items:
                  description: NodeProgress tracks one node's upgrade progress within
                    the group.
                  properties:
                    completedAt:
                      format: date-time
                      type: string
                    error:
                      type: string
                    fromVersion:
                      type: string
                    name:
                      type: string
                    phase:
                      type: string
                    startedAt:
                      format: date-time
                      type: string
                    toVersion:
                      type: string
                  required:
                  - name
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: NodeGroupUpgradePhase is the current phase of this group's
                  batch loop.
                enum:
                - Pending
                - Draining
                - Upgrading
                - Verifying
                - Complete
                - Failed
                - Paused
                type: string
              totalNodes:
                format: int32
                type: integer
              upgradedNodes:
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
