apiVersion: upgrade.k8s-upgrade-operator/v1alpha1
kind: KubernetesUpgrade
metadata:
  labels:
    app.kubernetes.io/name: kubernetes-upgrade-operator
    app.kubernetes.io/managed-by: kustomize
  name: kubernetesupgrade-sample
spec:
  # targetVersion is usually the only field you need to set - node groups,
  # their provider, and their upgrade strategy are discovered automatically.
  # See the README for the discovery mechanism and the escape hatches
  # (spec.groupOverrides, the upgrade.k8s-upgrade-operator/provider-override
  # annotation) for when the defaults don't fit your environment.
  targetVersion: "v1.30.0"
